Janelle Montgomery-4383646
CS3130
Assignment 1

This is a python program to administer an employee database. The program will present the user with a numberical list that will allow them to: add, delete, search, and display from the employee database.
The database accepts employee id's, names, and departments.

This assingment will include 2 python files and a database.txt file. The python files db_interaction where the code to carry out the options is included. It will have flags to catch invalid user inputs and messages to give reasoning to them. The db_main python file will have the code to provide the user with the menu to choose from as well as accept the user input. Again this will catch an invalid inputs.

